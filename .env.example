OPENAI_API_KEY=sk-your-key-here
# LLM model used for extraction and search (local OpenAI-compatible endpoint or API name)
# LLM_MODEL=gpt-5.6-luna
# Optional: path to a custom prompts JSON to tailor the LLM system prompts to a domain
# (defaults to the universal prompts in backend/prompts.json)
# PROMPTS_PATH=./prompts.medical.json
